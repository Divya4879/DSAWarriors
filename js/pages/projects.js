/**
 * Projects page component
 */

/**
 * Render the projects page
 * @param {HTMLElement} container - The container to render the projects page in
 */
function renderProjectsPage(container) {
  // Render the projects
  renderProjects(container);
}
