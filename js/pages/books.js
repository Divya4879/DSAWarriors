/**
 * Books page component
 */

/**
 * Render the books page
 * @param {HTMLElement} container - The container to render the books page in
 */
function renderBooksPage(container) {
  // Render the books
  renderBooks(container);
}
