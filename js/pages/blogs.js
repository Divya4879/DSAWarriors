/**
 * Blogs page component
 */

/**
 * Render the blogs page
 * @param {HTMLElement} container - The container to render the blogs page in
 */
function renderBlogsPage(container) {
  // Render the blogs
  renderBlogs(container);
}
